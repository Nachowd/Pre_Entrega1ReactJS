const ItemListContainer = ({greeting}) => {
    return (
        <div style={{fontFamily: "Calibri", fontSize: "25px", color: "Green"}}>{greeting}</div>
    )
    
};

export default ItemListContainer; 